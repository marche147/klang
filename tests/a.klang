function main() : int a, array b, string c, int d, int e, int f, int g, int m, int n -> int {
  b := array_new(4);
  a := 0;

  c := inputs();
  prints(c);

  do {
    if(a > 2) {
      b[a] := a * a * a * a * a;
    } else {
      b[a] := 1234;
    };
    a := a + 1;
  } while(a < 4);

  printi(b[3]);
  return 0;
}

function add(int a, int b) : -> int {
  return a + b;
}